function processCheckout() {
    alert("Chức năng thanh toán đang được phát triển!");
    // Thêm logic thanh toán tại đây
}

// Ví dụ: Cập nhật tổng số tiền (nếu có)
const cartTotal = document.querySelector(".cart__total-amount");
if (cartTotal) {
    cartTotal.textContent = "100000 VND"; // Giả lập tổng, thay bằng logic thực tế
}